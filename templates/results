<!-- fairmandering/templates/results.html -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Redistricting Results - Fairmandering</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <style>
        body { padding-top: 50px; }
        .container { max-width: 800px; }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center">Redistricting Results</h1>
        <hr>
        <h3>Fairness Metrics</h3>
        <ul>
            {% for metric, value in fairness_metrics.items() %}
                <li><strong>{{ metric.replace('_', ' ').title() }}:</strong> {{ value }}</li>
            {% endfor %}
        </ul>

        <h3>Analysis Results</h3>
        <ul>
            {% for key, value in analysis_results.items() %}
                <li><strong>{{ key.replace('_', ' ').title() }}:</strong> {{ value }}</li>
            {% endfor %}
        </ul>

        <h3>Ranked Plans</h3>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Plan</th>
                    <th>Total Score</th>
                </tr>
            </thead>
            <tbody>
                {% for index, row in ranked_plans.iterrows() %}
                    <tr>
                        <td>{{ index }}</td>
                        <td>{{ row['total_score'] }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <h3>Visualizations</h3>
        <div class="mb-3">
            <a href="{{ url_for('download_file', filename='district_map.html') }}" class="btn btn-info">Download District Map</a>
            <a href="{{ url_for('download_file', filename='fairness_metrics.png') }}" class="btn btn-info">Download Fairness Metrics Plot</a>
            <a href="{{ url_for('download_file', filename='district_populations.png') }}" class="btn btn-info">Download Population Distribution</a>
            <a href="{{ url_for('download_file', filename='population_trends.png') }}" class="btn btn-info">Download Population Trends</a>
            <a href="{{ url_for('download_file', filename='redistricting_report.txt') }}" class="btn btn-info">Download Report</a>
        </div>

        <a href="{{ url_for('home') }}" class="btn btn-secondary">Run Again</a>
    </div>
</body>
</html>
